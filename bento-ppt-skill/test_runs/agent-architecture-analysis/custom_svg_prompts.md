# Agent 底层架构分析 · Custom SVG Prompts

这份文件把本次试跑中 `render_mode=custom_svg` 的页面展开成可直接执行的页面提示词。

## Slide 1 · 封面

```text
作为精通信息架构与 SVG 编码的专家，你的任务是将完整的文字内容转化为一张高质量、结构化、具备高级感、简洁感和专业感的 SVG 演示文稿页面。

硬约束：
1. SVG viewBox 必须是 0 0 1280 720。
2. 页面应体现系统结构感，不要做成泛科技炫光海报。
3. 卡片间距至少 20px。

请根据以下页面规格输出 SVG 代码：

页面角色：cover
页面标题：Agent 底层架构分析
页面副标题：ReAct、Plan-and-Execute 与 Harness 的分层协作
页面内容：
- 主视觉建议体现观察、规划、执行三层结构
- 风格应偏系统图与结构秩序感，而不是科技炫光海报

额外风格要求：
- 标题必须成为第一视觉重心
- 只保留一个主视觉锚点，不要出现多组彼此竞争的图形
- 可以用三层结构示意、连线、抽象控制面板元素来隐喻 agent architecture
```

## Slide 2 · 目录

```text
作为精通信息架构与 SVG 编码的专家，你的任务是将完整的文字内容转化为一张高质量、结构化、具备高级感、简洁感和专业感的 SVG 演示文稿页面。

硬约束：
1. SVG viewBox 必须是 0 0 1280 720。
2. 使用结构清晰的目录页构图，不要堆满内容。
3. 卡片间距至少 20px。

请根据以下页面规格输出 SVG 代码：

页面角色：toc
页面标题：目录
页面副标题：从行动循环，到执行底座
页面内容：
- 为什么复杂任务会逼出分层代理
- ReAct 与 P&E 的职责边界
- Harness 为什么是系统底座
- 图表化测试与架构选择建议

额外风格要求：
- 目录建议采用 2x2 模块排布
- 每个目录项可以包含序号、标题、一个极短说明
- 画面应体现“分析型 deck”的秩序感
```

## Slide 3 · 章节页

```text
作为精通信息架构与 SVG 编码的专家，你的任务是将完整的文字内容转化为一张高质量、结构化、具备高级感、简洁感和专业感的 SVG 演示文稿页面。

硬约束：
1. SVG viewBox 必须是 0 0 1280 720。
2. 章节页是节奏页，不承载密集正文。

请根据以下页面规格输出 SVG 代码：

页面角色：chapter
页面标题：复杂任务为什么逼出分层
页面副标题：任务越长，架构问题越先暴露。
页面内容：
- 这一页只负责切节奏与定调，不承载密集正文

额外风格要求：
- 放大章节编号和章节名
- 使用一条极短的洞察句做视觉收束
- 背景可用抽象层级线框或结构节点作为隐喻
```

## Slide 9 · 图表测试 A

```text
作为精通信息架构与 SVG 编码的专家，你的任务是将完整的文字内容转化为一张高质量、结构化、具备高级感、简洁感和专业感的 SVG 演示文稿页面。

硬约束：
1. SVG viewBox 必须是 0 0 1280 720。
2. 这是一页图表导向页面，图表必须清晰、克制、可演示，不要做成 BI 仪表盘截图风格。
3. 卡片间距至少 20px。

请根据以下页面规格输出 SVG 代码：

页面角色：metric
页面标题：三种架构的能力雷达
页面副标题：测试图表效果：雷达图 + 分组柱状图 + 关键结论注释。
页面内容：
- 维度包含：速度、全局一致性、可控性、实现成本、可观测性
- ReAct 在速度高，P&E 在全局一致性高，Harness-backed Stack 在可控性与可观测性上更完整
- 右侧增加一个分组柱状图，展示三种架构在任务长度增加时的稳定性变化

额外风格要求：
- 左侧主图为能力雷达图，三条系列线分别对应 ReAct、P&E、Harness-backed Stack
- 右侧辅图为 grouped bar chart，对比短任务、中任务、长任务下的稳定完成率
- 不要添加过多刻度文字，保留 2-3 条关键注释即可
- 颜色区分要稳重，避免艳丽霓虹
- 页面整体仍然像演示文稿，而不是数据分析后台
```

## Slide 10 · 图表测试 B

```text
作为精通信息架构与 SVG 编码的专家，你的任务是将完整的文字内容转化为一张高质量、结构化、具备高级感、简洁感和专业感的 SVG 演示文稿页面。

硬约束：
1. SVG viewBox 必须是 0 0 1280 720。
2. 这是一页图表导向页面，重点是“复杂度变化带来的架构选择变化”。
3. 卡片间距至少 20px。

请根据以下页面规格输出 SVG 代码：

页面角色：metric
页面标题：任务复杂度与架构适配曲线
页面副标题：测试图表效果：复杂度曲线 + 阶段分区 + 推荐组合。
页面内容：
- 横轴为任务复杂度，纵轴为系统稳定完成率
- 曲线一：仅 ReAct，前期上手快但后段下滑明显
- 曲线二：ReAct + Harness，中段稳定性显著提升
- 曲线三：Planner + ReAct Worker + Harness，高复杂任务上限最高

额外风格要求：
- 主图为三条趋势曲线，表现不同架构在复杂度增加时的稳定性变化
- 用垂直分区带区分 simple、medium、hard 三个阶段
- 在每个阶段顶部用简短标签标注推荐架构组合
- 关键结论应直接写在图旁边，而不是埋在底部注脚里
- 画面要体现“架构选择随复杂度升级”的叙事感
```

## Slide 12 · 结尾页

```text
作为精通信息架构与 SVG 编码的专家，你的任务是将完整的文字内容转化为一张高质量、结构化、具备高级感、简洁感和专业感的 SVG 演示文稿页面。

硬约束：
1. SVG viewBox 必须是 0 0 1280 720。
2. 结尾页只保留一个结论中心，不要再扩写正文。

请根据以下页面规格输出 SVG 代码：

页面角色：closing
页面标题：从会做事，到能稳定做成事
页面副标题：Agent 架构的关键不是更会推理，而是更会组织执行。
页面内容：
- 一句强结论收束
- 可保留三条 takeaways
- 视觉上适合作为结束页，而不是正文页

额外风格要求：
- 保持克制，避免堆叠图表和模块
- 可以使用一条三层结构总结带，回收 ReAct、P&E、Harness 三个关键词
- 结尾的情绪应偏“结构升维”而不是“营销号口号”
```
